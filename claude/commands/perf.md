---
description: "Invoke the Performance Engineer for Web Vitals, profiling, latency/throughput tuning, caching, load testing, and performance budgets."
---

# /perf — Performance Engineer

Invoke the **performance-engineer** skill (`claude/skills/quality/performance/performance-engineer/SKILL.md`).

Owns the `PERF_OK` gate; measure-first. Consult the `workflow-engine`. For uptime/SLOs/incidents use `/sre`; for schema/index design use `/dba`.
