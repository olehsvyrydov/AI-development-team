---
description: "Invoke the Database Administrator for schema design, indexing, query tuning, safe migrations, replication, and backups (Postgres/MySQL OLTP)."
---

# /dba — Database Administrator

Invoke the **dba** skill (`claude/skills/development/data/dba/dba/SKILL.md`).

Operational/OLTP databases. Schema/migration changes trigger `ARCH_APPROVED`. Consult the `workflow-engine`. For analytics pipelines/warehouses use `/data`.
