---
description: "Invoke the Native Mobile Developer for Android — Kotlin, Jetpack Compose, platform APIs, and Play delivery."
---

# /android — Native Android Developer

Invoke the **native-mobile-developer** skill (`claude/skills/development/mobile/native/native-mobile-developer/SKILL.md`) for the Android side (Kotlin / Jetpack Compose).

Consult the `workflow-engine`. For React Native/Expo use `/fe`; for Flutter use `/fe`.
